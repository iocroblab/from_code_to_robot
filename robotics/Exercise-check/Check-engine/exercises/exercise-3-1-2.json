{
  "exercise": {
    "id": "3-1-2",
    "description": "Check Analytical Jacobian (ZYZ)",

    "variables": [
      {
        "description": "Check Ja numeric 6x6",
        "name": "exist('Ja','var') && isequal(size(Ja),[6,6]) && isa(Ja,'double')",
        "expected": true,
        "output_ok": "Ja is numeric and has the correct size",
        "output_fail": "There is an issue with the size of Ja or with the numeric computation"
      },
      {
        "description": "Rank is scalar",
        "name": "exist('Rank','var') && isscalar(Rank) && isa(Rank,'double')",
        "expected": true,
        "output_ok": "Rank is scalar",
        "output_fail": "Rank is not scalar"
      }
    ]
  }
}
